// Placeholder — the real login flow (POST /auth/login, session persistence)
// lands in Slice 2 (docs/PWA-PLAN.md).
export function Login() {
  return (
    <section>
      <h1>Sign in</h1>
      <p className="muted">Login arrives in the next slice.</p>
    </section>
  );
}
